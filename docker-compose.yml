version: '3.8'

services:
  dejoiypay:
    build: .
    container_name: dejoiy-pay
    ports:
      - "3030:3030"
    environment:
      - PORT=3030
      - PAYMENT_ENV=test
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
